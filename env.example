# Copy to Railway Variables or local .env
# Comma-separated list of one or more Discord webhook URLs:
WEBHOOK_URLS=https://discord.com/api/webhooks/ID1/TOKEN1,https://discord.com/api/webhooks/ID2/TOKEN2

# Default scrape endpoint (used if not overridden in JSON mode)
BSGO_URL=https://bsgo.fun/Services/Identity/Account/EU

# Schedule in minutes between runs
UPDATE_MINUTES=15
